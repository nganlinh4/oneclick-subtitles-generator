{
  "installCommand": "npm install --legacy-peer-deps --ignore-scripts && cd promptdj-midi && npm install --ignore-scripts",
  "buildCommand": "cd promptdj-midi && npm run build && cd .. && rm -rf public/promptdj-midi && mkdir -p public/promptdj-midi && cp -R promptdj-midi/dist/* public/promptdj-midi/ && CI=false npm run build:vercel",
  "outputDirectory": "build",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
